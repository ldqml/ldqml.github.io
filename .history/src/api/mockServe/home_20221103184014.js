import Mock, { mock } from 'mockjs'

let list = []
export default {
    getStateDate:()=>{
        for(let i=0; i<7;i++){
            list.push(
                Mock.mock({
                    
                })
            )
        }
    }
}